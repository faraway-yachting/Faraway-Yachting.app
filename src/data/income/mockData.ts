import {
  Quotation,
  Invoice,
  Receipt,
  CreditNote,
  DebitNote,
  BillingNote,
  Client,
} from './types';

// ============= CLIENTS (Empty - use Supabase) =============

export const mockClients: Client[] = [];

// ============= QUOTATIONS (Empty - use Supabase) =============

export const mockQuotations: Quotation[] = [];

// ============= INVOICES (Empty - use Supabase) =============

export const mockInvoices: Invoice[] = [];

// ============= RECEIPTS (Empty - use Supabase) =============

export const mockReceipts: Receipt[] = [];

// ============= CREDIT NOTES (Empty - use Supabase) =============

export const mockCreditNotes: CreditNote[] = [];

// ============= DEBIT NOTES (Empty - use Supabase) =============

export const mockDebitNotes: DebitNote[] = [];

// ============= BILLING NOTES (Empty - use Supabase) =============

export const mockBillingNotes: BillingNote[] = [];
